const { RecipeIngredients, Recipe, Ingredient } = require("../models");

const seedRecipeIngredients = async () => {
  //   const recipe = await Recipe.findByPk(1);
};

module.exports = seedRecipeIngredients;
